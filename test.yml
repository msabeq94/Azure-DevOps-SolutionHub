- task: AzureKeyVault@1
  inputs:
    ConnectedServiceName: '<Service Connection Name>'
    KeyVaultName: '<Your Key Vault Name>'
    SecretsFilter: '<Secret Name>'
    RunAsPreJob: true

- script: echo $(<Variable Name>)
  displayName: 'Use the secret'


escaped_key=$(echo ${terraformbackendkey} | sed -e 's/[&/\]/\\&/g' -e 's/=/\\=/g' -e 's/+//g')